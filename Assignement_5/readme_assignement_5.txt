Requirements
To run this application, you'll need:

1. Python 3.7+

2. Required packages:
# bash

    pip install pillow openai

3. An OpenAI API key with access to:
    -GPT-4 Vision model
    -DALL-E 3 model
